package com.example.brand;
import android.Manifest;
import android.app.Activity;
import android.content.Intent;
import android.content.pm.PackageManager;
import android.net.Uri;
import android.os.Bundle;
import android.support.v4.app.ActivityCompat;
import android.view.View;
import android.widget.Button;


public class my11 extends Activity {

    private static final int REQUEST_CALL =1 ;
    private static final int REQUEST_LOCATION =1 ;
    Button a,b,c,d,e,f,g,z;


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.my11);
a=(Button)findViewById(R.id.button13);
b=(Button)findViewById(R.id.button14);
c=(Button)findViewById(R.id.button15);
d=(Button)findViewById(R.id.button16);
e=(Button)findViewById(R.id.button17);
f=(Button)findViewById(R.id.button18);
        Bundle bundle=getIntent().getExtras();
        final String un=bundle.getString("un");
        final String pwd=bundle.getString("pwd");

c.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        Intent i=new Intent(my11.this,my2.class);
        Bundle bundle=new Bundle();
        String e=un;
        String f=pwd;
        bundle.putString("un",e);
        bundle.putString("pwd",f);
        i.putExtras(bundle);
        startActivity(i);
    }
});
        a.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent i=new Intent(Intent.ACTION_CALL);
                i.setData(Uri.parse("tel:100"));
                if(ActivityCompat.checkSelfPermission(my11.this, Manifest.permission.CALL_PHONE)!= PackageManager.PERMISSION_GRANTED){
                    ActivityCompat.requestPermissions(my11.this,new String[]{Manifest.permission.CALL_PHONE},REQUEST_CALL);

                }
                else startActivity(i);
            }
        });
        e.setOnClickListener(new View.OnClickListener() {
   